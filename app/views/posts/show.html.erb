<h1>投稿詳細</h1>
<p>
  <strong>名前：</strong><%= @post.name %>
</p>
<p>
  <strong>内容：</strong><%= @post.content %>
</p>
<p>
  <strong>作成日時：</strong><%= l(@post.created_at, format: :long) %>
</p>
<%= link_to "一覧に戻る", root_path %>
